# Spec: Pagination Component

## ADDED Requirements

### Requirement: Pagination Rendering

The system SHALL render pagination component using Figma token values.

#### Scenario: Default state

- **WHEN** a pagination component is rendered
- **THEN** the component SHALL use appropriate tokens for styling
