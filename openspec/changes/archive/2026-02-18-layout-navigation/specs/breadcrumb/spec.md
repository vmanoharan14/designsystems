# Spec: Breadcrumb Component

## ADDED Requirements

### Requirement: Breadcrumb Rendering

The system SHALL render breadcrumb component using Figma token values.

#### Scenario: Default state

- **WHEN** a breadcrumb component is rendered
- **THEN** the component SHALL use appropriate tokens for styling
